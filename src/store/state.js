/*
状态对象
 */
export default {
  token: '' || localStorage.getItem('token'), // 用户token信息
  username: '' || localStorage.getItem('username'), // 用户名称
  userid: '' || localStorage.getItem('userid'), // 用户id
}
